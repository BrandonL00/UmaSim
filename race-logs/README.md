# Race Logs

Use this folder for exported simulator run logs.

For now the app keeps only the latest run in browser storage and exports it as:

```text
latest-race-run.json
```

The browser cannot silently write into this project directory without a backend or a native file permission prompt, so move the exported JSON here when you want a workspace copy.
